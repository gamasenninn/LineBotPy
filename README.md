# LineBotPy
Line Bot (Python)


## 依存関係
```
pip install fastapi
pip install uvicorn[standard]
pip install openai
pip install linebot
pip install line-bot-sdk
pip install python-dotenv
pip install ipython
```

